<% if @current_user.present? %>
    <h2>Welcome <%= @current_user.email %>!</h2>

    <h3>Your teams:</h3>
    <% @current_user.teams.each do |team| %>
    <p>
        <%= link_to team.name, teams_path(team.id) %>
    </p>
    <% end %>
<% end %>